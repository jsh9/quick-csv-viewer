{
  "name": "quick-csv-viewer",
  "displayName": "Quick CSV Viewer",
  "description": "View and preview large CSV files in a readonly, fast VS Code custom editor.",
  "version": "0.0.1",
  "publisher": "jsh9",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/jsh9/quick-csv-viewer.git"
  },
  "bugs": {
    "url": "https://github.com/jsh9/quick-csv-viewer/issues"
  },
  "homepage": "https://github.com/jsh9/quick-csv-viewer#readme",
  "keywords": [
    "csv",
    "csv-viewer",
    "large-files",
    "table",
    "custom-editor"
  ],
  "galleryBanner": {
    "color": "#1f6feb",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/src/extension.js",
  "activationEvents": [
    "onCustomEditor:quickCsvViewer.viewer",
    "onCommand:quickCsvViewer.openCurrentFile",
    "onCommand:quickCsvViewer.openSampleFiles"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    },
    "virtualWorkspaces": false
  },
  "contributes": {
    "commands": [
      {
        "command": "quickCsvViewer.openCurrentFile",
        "title": "Open in Quick CSV Viewer",
        "category": "Quick CSV Viewer"
      }
    ],
    "languages": [
      {
        "id": "csv",
        "aliases": [
          "CSV",
          "csv"
        ],
        "extensions": [
          ".csv"
        ]
      }
    ],
    "customEditors": [
      {
        "viewType": "quickCsvViewer.viewer",
        "displayName": "Quick CSV Viewer",
        "selector": [
          {
            "filenamePattern": "*.csv"
          },
          {
            "filenamePattern": "**/*.csv"
          }
        ],
        "priority": "default"
      }
    ],
    "configurationDefaults": {
      "workbench.editorAssociations": {
        "*.csv": "quickCsvViewer.viewer"
      }
    },
    "configuration": {
      "title": "Quick CSV Viewer",
      "properties": {
        "quickCsvViewer.maxRows": {
          "type": "integer",
          "default": 20,
          "minimum": 0,
          "description": "Number of CSV data rows to show. Set to 0 to index the full file and render visible rows on demand."
        },
        "quickCsvViewer.firstRowIsHeader": {
          "type": "boolean",
          "default": true,
          "description": "Treat the first CSV record as column headers and exclude it from the displayed row count."
        },
        "quickCsvViewer.wrapCellContents": {
          "type": "boolean",
          "default": true,
          "description": "Wrap CSV cell contents in the table view."
        }
      }
    },
    "menus": {
      "commandPalette": [
        {
          "command": "quickCsvViewer.openCurrentFile"
        }
      ],
      "editor/title": [
        {
          "command": "quickCsvViewer.openCurrentFile",
          "when": "resourceExtname == .csv",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "quickCsvViewer.openCurrentFile",
          "when": "resourceExtname == .csv",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "clean": "node scripts/clean.cjs",
    "compile": "tsc -p ./",
    "compile:clean": "npm run clean && npm run compile",
    "format": "prettier . --write --ignore-unknown",
    "format:check": "prettier . --check --ignore-unknown",
    "prepare": "husky",
    "test": "npm run format:check && npm run compile:clean && npm run test:coverage",
    "test:coverage": "c8 --all --src out/src --include 'out/src/**/*.js' --check-coverage --lines 95 --branches 95 --functions 95 npm run test:unit",
    "test:unit": "node scripts/run-tests.cjs",
    "test:vscode": "npm run compile:clean && vscode-test",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.14.8",
    "@types/vscode": "1.90.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^3.0.0",
    "c8": "^11.0.0",
    "husky": "^9.1.7",
    "mocha": "^11.7.6",
    "prettier": "^3.8.4",
    "typescript": "^5.8.3"
  }
}
